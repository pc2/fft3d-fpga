# Compilation Flags

- DEBUG: to activate writing to stdout
  e.g. make DEBUG=1

# Runtime Flags for FFT3d
- -m     : size of first dimension
- -n     : size of second dimension
- -p     : size of third dimension
- -b     : compute backward/inverse transforms
- -i     : number of iterations to average performance
